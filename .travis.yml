sudo: false
install: true
language: android
jdk: oraclejdk8
android:
  components:
    - tools
    - platform-tools
    - build-tools-29.0.0
    - android-29
    - extra-android-support
    - extra-google-google_play_services
    - extra-android-m2repository
    - extra-google-m2repository

  licenses:
    - '.+'

before_install:
  - chmod +x gradlew
  - ./gradlew dependencies || true
  
script:
  - ./gradlew :app:assembleRelease --stacktrace

after_success:
  - curl -T $TRAVIS_BUILD_DIR/app/build/outputs/apk/release/app-release-unsigned.apk ftp://haleclipse:Y4kBNS45EdFzKp10f7Rc0ocCdJzq1qLGQw9EtNltkSxWsmGhwYesGgm3SNFf@ftp.gear.host/site/wwwroot/
